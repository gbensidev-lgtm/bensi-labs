# Supabase — Bensi Labs Studio
# Copie este arquivo para .env.local e preencha no dashboard do Supabase.
# Nunca versionar valores reais. Nunca usar a service role no client.
# A service role é usada só no servidor para gravar o intake público.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
